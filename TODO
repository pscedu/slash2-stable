make
- rename ROOTDIR -> PROJ_BASE
- introduce APP_BASE

general
- watch out for sizeof(type var[0])
- constify RPC replies in clients and requests in servers

slash
- piggyback small readdirs directly into READDIR replies
- remove generation numbers from slash_fid_t and add a new slash_fidgen_t
- remove rpc_sendmsg

pfl
- remove psclist_add
- no ifdefs
- disallow lock reinitialization (e.g. allow LOCK_INIT once)
- remove psclist_del_init

lnet-lite
- remove ZEST_LNET
- add ${LNET_SOCKLND_SRCS} vars as a replacment for LNET_LIBS
