make
- rename ROOTDIR -> PROJ_BASE
- introduce APP_BASE

general
- watch out for sizeof(type var[0])

slash
- piggyback small readdirs directly into READDIR replies
- remove generation numbers from slash_fid_t and add a new slash_fidgen_t

pfl
- remove psclist_add
- psclist_for_each* should be changed to NULL out the iterative variable
  at loop end
- no ifdefs
- disallow lock reinitializatoin (e.g. allow LOCK_INIT once)
- remove psclist_del_init

lnet-lite
- remove ZEST_LNET
